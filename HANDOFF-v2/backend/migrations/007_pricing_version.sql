-- Записываем, по какой модели цен создана версия результата.
-- 1 = старая схема (цена по операциям), 2 = "1 кредит = 1 проход AI" (23.09.2026).
-- Старые записи остаются NULL и трактуются как модель 1 в отчётах.
ALTER TABLE project_versions ADD COLUMN pricing_version INTEGER;
