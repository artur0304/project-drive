# 00_READ_ME_FIRST — ПРОЧИТАЙ ЭТО ЦЕЛИКОМ ПЕРЕД ЛЮБЫМ ДЕЙСТВИЕМ

Ты подключаешься к проекту **Project Drive** — AI-сервису визуального тюнинга машин.
Пользователь (Артур) уже полностью прошёл дизайн-фазу и часть бэкенда со мной (Claude).
Всё, что было решено и сделано, — в этом пакете. Прочитай этот файл целиком, потом
открой упомянутые файлы. Не изобретай заново то, что уже решено — ниже указано, что
именно и почему.

---

## 🛑 РАЗДЕЛ 0 — САМОЕ ВАЖНОЕ. ПРОЧИТАЙ ДВАЖДЫ. ДЕНЬГИ.

У Артура **два Google-аккаунта**, и их легко перепутать — путаница уже случалась.

- **Старый Google-аккаунт (адрес удалён из репозитория).** На нём **$100 предоплаченных
  Gemini-кредитов**. Артур случайно их купил, обратился в поддержку Google.
  Поддержка (агент Nibin) оформила разовую "courtesy adjustment" — попытку вернуть
  деньги, но **не гарантированную**. Ответ по заявке обещали через 2–3 рабочих дня,
  и если одобрят — сам возврат на карту займёт ещё 7–10 рабочих дней. Номер
  обращения хранится только у владельца и в переписке с поддержкой, не в Git.
  Официально по политике Google эти кредиты невозвратны, но человек
  из поддержки всё равно подал заявку на разовое исключение.

- **Новый рабочий Google-аккаунт (адрес удалён из репозитория).** Это аккаунт проекта на
  будущее. Биллинг там **ещё не настроен**. Минимальная сумма первого пополнения
  через Google AI Studio — **A$40** (это для Артура сейчас много, он отказался
  платить). Бесплатного режима генерации картинок в AI Studio Playground больше нет
  (проверено — упирается в апгрейд/биллинг). То есть на новом аккаунте баланс = $0
  и способа сгенерировать бесплатно не нашлось.

### ПРАВИЛО, ОБЯЗАТЕЛЬНОЕ К ИСПОЛНЕНИЮ:

**НИ ОДИН из этих двух аккаунтов НЕЛЬЗЯ использовать для реальных вызовов Gemini
(генерации картинок, тестов, bake-off — ничего) без свежего, явного разрешения
Артура, данного в моменте.** Не "он разрешил раньше" — именно сейчас, на этот
конкретный запуск. Это не постоянный запрет — это пауза до его решения (ожидается
в течение нескольких рабочих дней от 19 сентября 2026). Но пока он не сказал явно
"давай, генерируй" — **не трогай ни один из ключей, не включай биллинг, не жми
Generate нигде в AI Studio, не предлагай "давай я по-быстрому проверю"**.

По состоянию на 22.09 выбран рабочий провайдер **fal.ai**, кошелёк fal пополнен
отдельно, а единственная модель — **Nano Banana 2**. Серверный адаптер уже написан,
но режим по умолчанию остаётся **`mock`**. `live` нельзя включать и платный вызов
нельзя делать без свежего явного разрешения Артура на конкретный запуск. Оба
Google-аккаунта и $100 Gemini при этом по-прежнему не используются ни при каких
условиях до отдельного решения Артура.

---

## РАЗДЕЛ 1 — КАК РАБОТАТЬ С АРТУРОМ (важно не меньше кода)

Это не формальность — от этого зависит, будет ли ему с тобой комфортно и продуктивно.

- **Он начинающий программист** (опыт — PHP, есть учебный проект на GitHub
  `artur0304/cuecraft`). Также хочет по ходу подтягивать английский.
- **Весь код — с подробными комментариями простым языком.** Это не разовая просьба,
  а постоянное правило. Комментарии — по-русски, идентификаторы в коде — по-английски.
- **Объясняй просто, без "программистского" жаргона.** Если без термина не обойтись —
  объясни его тут же в скобках. Не предполагай, что он знает, что такое webhook,
  API, токен и т.д., пока сам не покажет, что знает.
- **Не забегай вперёд плана.** Он много раз ловил меня на попытках сделать
  "заодно побольше" — и просил тормозить. Работайте строго по фазам (раздел 4).
  Не добавляй функции, которые в текущей фазе не нужны, даже если "напрашивается".
- **Он не хочет сам ставить программы и писать команды в терминале.** Тебе, в отличие
  от меня, доступен его компьютер — значит именно ТЫ ставишь Node, создаёшь папки,
  запускаешь сервер, показываешь готовый результат. Не присылай ему пошаговые
  инструкции "открой терминал, набери команду" — сделай это сам и покажи итог.
- **Спрашивай перед необратимым или платным действием.** Особенно — перед всем,
  что касается денег (см. Раздел 0), перед удалением, перед развёртыванием
  куда-либо в открытый доступ.
- **Веди диалог маленькими шагами.** Предлагай варианты с плюсами/минусами и своей
  рекомендацией, а не стену текста. Когда он говорит "делай"/"++"/"погнали" —
  не переспрашивай лишний раз, действуй.
- Он может нервничать и путаться в деньгах и аккаунтах (см. всю сагу с Google) —
  объясняй денежные механики очень конкретно, по шагам, без сокращений.

---

## РАЗДЕЛ 2 — ЧТО ЗА ПРОЕКТ

AI-веб-сервис визуального тюнинга **реальной машины пользователя**. Человек
загружает ОДНО фото своей машины и примеряет: цвет/плёнку кузова, тонировку,
перекрас дисков, замену дисков — на СВОЁМ фото, максимально сохраняя ракурс, фон,
свет, геометрию. Это НЕ генератор машин по фантазии и НЕ замена машины 3D-моделью.

Позиционирование: **"See it on your car first."** Полный оригинальный бриф от
Артура — `docs/master_brief_original.md`, визуальный референс дизайна интерьера
конфигуратора (Wrap/Tint/Wheels/Wheel Color, bottom sheet и т.д.) —
`docs/project_drive_visual_reference.html`. Они частично устарели по деталям
(см. раздел 3), но информационная архитектура оттуда верна и легла в основу.

**MVP — ровно 4 операции:** wrap (плёнка/цвет кузова), tint (тонировка),
wheel recolor (перекрас дисков), wheel replace (замена дисков).
Не входит в MVP: spoilers, обвесы, оптика, занижение — и вообще ничего геометрическое.

---

## РАЗДЕЛ 3 — ЧТО УЖЕ СДЕЛАНО (не переделывать)

### 3.1 Дизайн (папка `design/`)

Пять экранов, в едином стиле, HTML-прототипы (открываются в браузере как есть,
никаких зависимостей кроме Google Fonts):

- `01-landing.html` — главная. **Особенность:** скролл — это таймлайн: машина
  в герое буквально трансформируется по мере прокрутки (меняет цвет, диски,
  темнеют стёкла), заголовки сменяются синхронно. Это "фирменный приём" страницы.
- `02-upload.html` — экран загрузки фото (drag&drop + переключение в состояние
  предпросмотра после выбора файла).
- `03-configurator.html` — ГЛАВНЫЙ рабочий экран. Слева машина, справа панель
  (вкладки Wrap/Tint/Wheels/Wheel color). **Критичная бизнес-логика:** выбор
  варианта НЕ меняет машину на экране сразу — меняется только "черновик" (подсветка
  выбора + список выбранного с ценой + итоговая сумма). Машина реально меняется
  ТОЛЬКО после нажатия Generate. Это сделано намеренно: если бы человек бесплатно
  видел точный результат до оплаты — незачем платить за генерацию. НЕ ломай эту
  логику при доработках. Стоимость считается как сумма по всем выбранным
  операциям и показывается построчно + итог (числа условные — см. раздел 3.3).
- `04-result.html` — экран результата: ползунок before/after, кнопки Download,
  Regenerate (сделать заново, платно — для случаев плохого качества), Duplicate as
  new variation (сделать копию текущего результата и пробовать другое, не теряя
  оригинал), Continue editing, Share link, Save to garage.
- `05-garage.html` — "гараж": машины пользователя и их сохранённые версии
  карточками. Фон — рассеянные по всей высоте страницы чертежи (машины, диски,
  суппорты, размерные линии) тем же акцентным цветом, генерируются JS так, чтобы
  при скролле открывались НОВЫЕ детали, а не повтор.
- `00-all-screens-preview.html` — все пять экранов в одном файле с переключателем
  сверху (iframe-свитчер) — просто для удобного просмотра, не финальная навигация.

**Дизайн-система (важно соблюдать на всех новых экранах):**
- Тёмный фон с лёгкой синевой, НЕ чистый чёрный: `#0C0E13` / `#12151B` / `#171B22`.
- Текст: `#F1EEE8` основной, `#8B93A0` вторичный (подкрашен, не плоский серый).
- **Один акцент** — тёплый красный `#E5533B`. **НЕ золото/латунь** — это
  сознательный уход от золотой палитры первой версии (см. ниже про scroll-craft).
- Шрифты: заголовки — **Space Grotesk**, текст — **Manrope**.
- Глубина — через тень/свет/лёгкое зерно (grain), не через яркие градиенты.
- Чертёжный/blueprint-фон — уместен ТОЛЬКО на атмосферных экранах (Garage,
  можно Upload). На рабочих экранах (Configurator, Result) фон должен быть тихим/
  отсутствовать — там человек концентрируется на машине, а не на декоре фона.

**Про методологию scroll-craft (важно):** Артур дал референс
`github.com/nateherkai/scroll-craft` — это плагин-навык именно для Codex/Claude Code
(ставится командой `/plugin marketplace add nateherkai/scroll-craft`, затем
`/plugin install nateherk-design`), который умеет строить премиальные
scroll-driven сайты и **сам себя проверяет** (headless browser проходит по всем
позициям скролла и ищет "мёртвый" скролл, недостаточный контраст и т.д.). Он также
даёт чёткий список запретов: одинаковые сетки карточек, счётчики "01/06", scroll-cue
стрелки, градиентный текст, выдуманная статистика, **и золото-кремовая "ремесленная"
палитра** (та, что была у нас изначально — поэтому и ушли от неё).

`01-landing.html`, который есть в пакете, — это МОЯ РУЧНАЯ имитация приёма
scroll-craft (я не могу установить и запустить сам плагин — там нужны Node, ffmpeg,
Playwright, чего нет в моей песочнице). Артуру этот вариант понравился по
направлению. **Рекомендация: если у тебя (Codex/Claude Code) есть возможность
установить и использовать реальный scroll-craft — предложи Артуру пересобрать
лендинг через него по-настоящему**, а мой `01-landing.html` использовать как
референс направления (палитра, идея "скролл двигает трансформацию машины"), не
как финальный код для копирования один в один.

### 3.2 AI-ядро (папка `ai-core-slice0/`, TypeScript, фреймворк-независимое)

Это "мозги" генерации, ПОЛНОСТЬЮ написаны, НЕ запущены на реальном AI (см. Раздел 0).

- `src/types.ts` — типы операций/конфигов/результатов/логов.
- `src/instructions.ts` — собирает structured draft → текст-инструкцию для AI с
  явным "сохрани всё остальное" + прикладывает reference-картинки диска.
- `src/providers/provider.ts` — интерфейс `AICarEditProvider.generateCarEdit()`.
- `src/providers/mockProvider.ts` — заглушка для тестов (без сети/денег).
- `src/providers/geminiProvider.ts` — **РЕАЛЬНЫЙ** адаптер под Gemini/Nano Banana
  (endpoint, заголовок авторизации `x-goog-api-key`, формат запроса — сверены по
  актуальной документации на момент написания). **НЕ ВЫЗЫВАТЬ без разрешения из
  Раздела 0.** Модель по умолчанию — `gemini-3-pro-image-preview`, дешёвый вариант
  для сравнения — `gemini-3.1-flash-image`. Имена моделей стоит перепроверить в
  AI Studio перед использованием — они меняются.
- `src/providers/openaiProvider.ts` — шаблон-заготовка под альтернативного
  провайдера (endpoint НЕ подтверждён, помечено TODO — не использовать не проверив).
- `src/providers/index.ts` — реестр провайдеров + роутинг по операции + fallback.
- `src/orchestrator.ts` — сердце: primary → retry(1) → fallback(1), полный лог
  попыток/стоимости/задержки.
- `src/bakeoff/rubric.ts` — 8 критериев качества, порог принятия рендера ≥4/5,
  порог "фича проходит" ≥70% принятых.
- `src/bakeoff/runBakeoff.ts` — harness для прогона фото×операции×провайдеры,
  выдаёт JSON/CSV + шаблон для ручной оценки качества.

Всё это готово к запуску bake-off'а **как только Артур разрешит тратить деньги**
(см. `docs/bakeoff_prep_photos_and_prompts.md` — готовый список того, какие фото
снять, и готовые промпты под все 4 операции).

### 3.3 Бэкенд (папка `backend/`, обычный Node.js, БЕЗ внешних зависимостей)

Написан и **проверен вживую** (запускался и тестировался в песочнице — не на
реальном сервере, не в интернете, никаких денег не потрачено). Полностью рабочий
локальный API. Подробности и список маршрутов — в `backend/README.md`, там же
все команды запуска.

Что внутри и проверено:
1. **База данных** (`schema.sql` + `db.mjs`) — SQLite (в проде по плану — Postgres,
   функции написаны так, чтобы менялось только тело, не сигнатуры). Таблицы:
   users, car_projects, source_assets, project_versions, credit_wallets,
   credit_transactions, orders.
2. **Вход по почте+паролю** (`auth.mjs`) — регистрация, вход, пароль хранится как
   соль+scrypt-хэш (не в открытом виде), выдаются токены-сессии. **Google OAuth
   НЕ реализован** — Артур пока явно просил делать только почту+пароль. Каркас под
   Google можно добавить отдельным шагом, когда он подтвердит.
3. **Все проекты/кошелёк/загрузки привязаны к вошедшему пользователю** через
   токен сессии (не через переданный клиентом userId) — это была сознательная
   правка безопасности (изначально можно было запросить чужие данные, подставив id).
4. **Загрузка фото** (`/api/upload`) — принимает JPG/PNG/WEBP/HEIC до 20MB,
   привязывает к своему проекту. `sharp` применяет ориентацию, ограничивает
   большую сторону 4096px, конвертирует результат в JPEG и удаляет EXIF/GPS.
   Неподдерживаемый вариант HEIC получает понятную ошибку 422.
5. **Оплата — только КАРКАС** (`/api/checkout`, `/api/webhook/payment`) — создаёт
   заказ, "оплата прошла" начисляет кредиты. **Провайдер — заглушка.**
   Идемпотентность проверена (повторный вебхук не задваивает начисление).
   **TODO(prod), критично:** в вебхуке ОБЯЗАТЕЛЬНО добавить проверку подписи от
   платёжки — без этого кто угодно сможет "начислить" себе кредиты. Провайдер
   ещё не выбран Артуром окончательно (направление — merchant-of-record, кандидаты
   Lemon Squeezy / Paddle, проверял доступность выплат в Украину — сам не решил).
6. **Генерация замкнута НА ЗАГЛУШКЕ** (`generation.mjs`) — сервер сам считает
   цену по операциям (клиенту не доверяем), списывает кредиты ДО вызова AI,
   вызывает `mockGenerate()` (просто возвращает исходное фото с задержкой), при
   успехе сохраняет версию, **при неудаче возвращает кредиты**, при нехватке
   кредитов — отказ с кодом 402. Всё это проверено тестами (см. README).
   **Когда Артур разрешит — заменить `mockGenerate()` на вызов реального
   `geminiProvider.ts` из ai-core-slice0.** Интерфейс входа/выхода уже совместим.

Цены операций в `generation.mjs` (PRICE) — **условные-заглушки**, подставлены для
работы логики. Настоящие цифры — только после bake-off на реальном AI (см. Раздел 0).

### 3.4 Текущее состояние продукта (обновлено 21.09.2026)

Главная страница содержит четыре scroll-состояния одной машины (stock, wrap,
wheels/tint, finished); sticky-анимация исправлена и не исчезает после первого экрана.

В папке `web/` уже работает единая локальная цепочка:

1. Landing (`/`) больше не загружается через iframe: он перенесён в React.
   Закреплённая SVG-машина меняет плёнку, тонировку и диски по скроллу, а CTA
   ведут напрямую в Upload (`/upload`) и Garage (`/garage`).
2. Регистрация/вход через локальный API по почте и паролю.
3. Создание проекта, загрузка исходника и запуск через серверный режим `mock/live`.
   По умолчанию и после тестов остаётся `mock`; `live` включается только для
   отдельно разрешённых запусков.
4. Страница результата с Before/After и сохранением версии в SQLite.
5. React Garage (`/garage`) читает проекты, исходники, версии и демонстрационный
   баланс из SQLite. Из него можно открыть сохранённый результат или вернуть
   исходное фото и последний draft в конфигуратор для новой вариации.
6. Credits (`/credits`) показывает локальный баланс и полный журнал операций.
   Черновые пакеты видны только для компоновки: checkout заблокирован до bake-off.
   Account (`/account`) показывает локальный профиль, число проектов и даёт выйти
   из локальной сессии. Garage, Credits и Account соединены общей навигацией.
7. Upload, Configurator и Result показывают фактический локальный баланс из
   SQLite вместо старой макетной цифры `103`. Начало загрузки новой машины очищает
   только указатели текущего проекта в браузере, поэтому создаст новый проект,
   но не удалит прежнюю машину или версии из Garage.
8. Проект машины можно переименовать прямо в Garage. Серверный `PATCH` проверяет
   владельца по локальной сессии; тестовый проект переименован из `photo-2` в
   `BMW X5`, исходник и сохранённая версия остались без изменений.
9. Старую сохранённую версию можно открыть и продолжить редактировать: Result
   восстанавливает её исходное фото в IndexedDB, project id и draft, а затем
   открывает Configurator. Поток не зависит от случайно оставшегося файла.
10. На Result есть `Report bad result`: причина и комментарий сохраняются локально
    у конкретной версии в SQLite. Сервер проверяет владельца; повторная жалоба не
    дублируется. Автоматического возврата кредитов нет — только ручная проверка.
11. Для возвращающегося пользователя добавлен отдельный `/login`: вход или
    регистрация ведут сразу в Garage. Landing показывает `Account` при активной
    локальной сессии и `Sign in` без неё. Google OAuth не используется.
12. После ревью безопасности цены операций имеют один источник в
    `backend/generation.mjs` и отдаются через `GET /api/pricing`. Клиентский
    `/api/wallet/add` удалён; тестовые кредиты добавляются только ручным скриптом.
    Backend слушает `127.0.0.1`, а каркас платёжного webhook дополнительно проверяет
    loopback до появления настоящей проверки подписи провайдера.
13. Результат открывается по постоянному адресу `/result/[versionId]` и читается
    из API после проверки владельца. Configurator разделён на компоненты, запросы
    и ключи browser storage собраны в `web/app/lib/`. Добавлены изолированные
    тесты денежной цепочки и smoke-тест расчёта конфигуратора.
14. Добавлен масштабируемый каталог дисков: поиск, фильтры, cursor-пагинация,
    избранное, recent и виртуализированная сетка. Есть админка CSV-импорта;
    публикация невозможна без явной отметки прав на изображение.
15. Мобильный Configurator использует bottom sheet с состояниями Peek/Half/Full.
16. Многошаговая генерация спроектирована как план операций с частичным возвратом
    кредитов за неуспешные шаги. Реальный AI этим планом ещё не вызывался.
17. Публичное превью результата выключено по умолчанию. Владелец сам включает
    ссылку; публичная версия получает водяной знак, оригинал доступен владельцу.
18. Upload выполняет preflight до списания кредитов: отклоняет повреждённые,
    слишком маленькие и чрезмерно узкие изображения.
19. Добавлены локальные продуктовые события и воронка, просмотр последних событий
    в админке и приватный экспорт данных аккаунта.
20. Каждый запрос получает `X-Request-ID`; структурные логи не содержат тела,
    заголовков и токенов. `/api/ready` проверяет доступность и целостность SQLite.
21. Bake-off подготовлен как безопасный конвейер: валидация датасета, полная
    матрица, сохранение артефактов, слепая ручная оценка и mock dry-run. Платные
    провайдеры по умолчанию заблокированы.

### 3.5 Решение 22.09: fal.ai + Nano Banana 2 и каталоги

Артур провёл внешние живые сравнения и выбрал единственную рабочую модель:
`fal-ai/nano-banana-2/edit` через fal.ai. Роутинг между моделями и cheap-first
отменены: экономия слабой моделью приводит к лишним исправлениям. FLUX не входит
в рабочую цепочку. Google $100 по-прежнему не используется.

Backend теперь содержит реальный `fal-provider.mjs`, но `PROJECT_DRIVE_AI_MODE`
по умолчанию равен `mock`. Добавлены: серверная сборка prompt из catalog id,
фиксированный `FIDELITY_CLAUSE_V1`, 50 вариантов плёнки, 6 уровней и 4 зоны
тонировки, 8 вариантов цвета дисков, prompt-фрагменты и тип спиц для каталога
дисков. Свободный AI-текст спрятан в Advanced, основной путь — выбор мышкой.

Денежные защиты: $5 lifetime, $2/day, лимит попыток по пользователю и IP,
локальный spend-ledger, кэш по хэшу исходника + option id + prompt version,
фактическая себестоимость версии и отказ от практически неизменившейся картинки.
Референс диска идёт вторым изображением; fal URL кэшируется в БД. Публикация
нового reference требует ручного подтверждения прав и отсутствия водяного знака.

Проверенная локальная тестовая запись: одна машина, одна версия, остаток 48
демонстрационных кредитов. Эти кредиты ненастоящие; Google, Gemini, API-ключи и
платёжные аккаунты в этой цепочке не используются. Production-сборка `web/`
проходит успешно. Все пять исходных статичных экранов перенесены в React и
подключены к локальному API. Навигация Garage, Credits и Account вынесена в общий
компонент; активный раздел и демонстрационный баланс отображаются одинаково.
Реальный адаптер подключён в коде. 23.09 сделан один разрешённый платный вызов из
приложения за $0.08; он успешно создал Deep Navy Metallic. Постоянный режим всё
равно остаётся `mock`, а `live` включается только для разрешённых партий.

Общий клиентский `apiRequest` удаляет просроченный локальный токен при любом
ответе `401`. Денежные и прикладные ошибки (`402`, `4xx`, `5xx`) не завершают
сессию; это поведение закреплено frontend-тестами.

### 3.6 Что НЕ проверено

1. Первый интегрированный вызов Nano Banana 2 из интерфейса выполнен 23.09:
   `Deep Navy · Metallic`, 16,25 с, $0.08, 25 демонстрационных кредитов. Результат
   сохранён и открыт на Result. Подробности: `HANDOFF-v2/docs/STAGE2_LIVE_REPORT.md`.
2. Первый реальный результат дал среднюю разницу 0.05331. Общий порог 0.012 ещё
   нельзя считать откалиброванным: нужны шесть вызовов фазы B по разным операциям.
3. Один комбинированный заказ на Mercedes (плёнка + тонировка + перекрас дисков)
   выполнен одним шагом `combined`: 16,54 с и $0.08. Три простые правки не
   увеличили AI-себестоимость относительно одиночной плёнки. Общий живой расход
   проекта на этот момент — $0.16.
4. HEIC-нормализация не проверена на будущей production-ОС.
5. Платёжный провайдер не выбран, подпись webhook не реализована, поэтому реальные
   платежи включать нельзя.
6. SQLite подходит только для локальной разработки; миграция на PostgreSQL ещё не
   выполнялась.
7. Публичные ссылки, каталог и аналитика проверены тестами и локально, но не
   реальными пользователями и не интернет-нагрузкой.

### 3.7 Правило актуальности документации

Любой коммит, который меняет поведение продукта, обязан в том же коммите обновить
этот файл. Откладывать обновление handoff-документа на потом нельзя.

### 3.8 Устойчивость генерации (25.09.2026)

- Списание кредита и создание задания `pending` теперь атомарны: между ними нет
  окна, в котором падение сервера могло бы оставить пользователя без кредита.
- При запуске backend возвращает кредиты за старые незавершённые задания ровно
  один раз. Порог по умолчанию — 10 минут, настраивается через `.env`.
- Вызов fal имеет таймаут 90 секунд. Внутренний кредит при таймауте возвращается,
  а SpendGuard после отправки запроса консервативно сохраняет возможный расход fal.
- Во время ожидания интерфейс показывает число запланированных AI-проходов и
  предупреждает не закрывать окно.
- Проверки на Windows: backend 16/16, web 14/14 и production build Next.js прошли.
- Два BAT-файла запуска используют встроенный Node 22 и должны храниться в Git.

### 3.9 Подготовка к деплою и правовые черновики (25.09.2026)

- Чтение и запись изображений вынесены в `backend/object-storage.mjs`. Локальный
  адаптер сохраняет прежнее поведение; будущий R2/S3 должен реализовать тот же API.
- Добавлен тест хранилища: запись/чтение, read-only каталога и защита от traversal.
- `docs/DEPLOY_OPTIONS.md` фиксирует рекомендуемую схему, точные шаги PostgreSQL,
  приватный R2/S3, signed URL и checklist. Реального деплоя и облачных расходов нет.
- Добавлены черновики Terms и дисклеймера по тонировке; privacy draft расширен.
  Все три документа требуют юриста, реальных контактов и сроков хранения.
- Платежи остаются выключенными и не разрабатывались.

### 3.10 Пользовательская проверка Playwright (25.09.2026)

- Главная, загрузка фото, конфигуратор, регистрация/вход, mock-генерация,
  Result, Garage, Credits и Account пройдены через официальный Playwright CLI.
- Проверены формы, навигация, фильтры каталога, сравнение до/после, скачивание,
  публичная ссылка, жалоба и размеры 1440×900 и 390×844.
- Исправлены отсутствующий favicon, имя поля поиска дисков для assistive
  technology, вывод внутреннего UUID в кредитной истории и форма `1 version`.
- После исправлений: web-тесты 14/14, production build Next.js и чистая консоль
  Playwright прошли. Финальные скриншоты лежат в `docs/playwright-qa/`.
- Проверка использовала только локальную mock-генерацию; платный AI не вызывался.

### 3.11 Визуальная полировка интерфейса (25.09.2026)

- Добавлена общая холодная automotive-система токенов: типографика, цвета,
  фокус, выделение текста, scrollbar, радиусы и единые motion-кривые.
- Лендинг получил асимметричную композицию, компактную плавающую навигацию,
  более ясную историю продукта и собранный блок закрытой беты.
- Upload, Configurator и Result приведены к одной визуальной иерархии без
  изменения маршрутов, API, данных и расчёта кредитов.
- Микровзаимодействия ограничены feedback/state indication: нажатия, выбранные
  варианты, модальное окно авторизации. Учтён `prefers-reduced-motion`.
- Impeccable-детектор выявил и помог убрать декоративную сетку Upload и анимацию
  высоты mobile-sheet. Playwright повторно проверил desktop/mobile, навигацию,
  вкладки, размеры панели и Result; ошибок console нет.
- Финальные изображения сохранены в `HANDOFF-v2/docs/playwright-qa/polish-*.png`.
  Платные AI/API не использовались.

---

## РАЗДЕЛ 4 — ПЛАН ФАЗ (строго по порядку, не пропускать и не объединять)

**Текущий шаг — проверить интеграцию fal.** Код, каталоги, защита бюджета и
автотесты готовы. Первый вызов Nano Banana 2 на 1K успешно выполнен за $0.08.
Следующий шаг — Артур оценивает картинку, затем начинается шестизапросная
калибровка фазы B в рамках отдельно одобренного бюджета этапа 2. Сравнение
моделей больше не требуется.

**Slice 1 (локальный продукт в основном готов):**
- Google OAuth как вторая дверь входа (когда Артур подтвердит).
- Проверить HEIC на целевой серверной ОС; EXIF-strip и JPEG-нормализация через
  `sharp` уже подключены локально.
- Пять экранов уже перенесены в Next.js и связаны с локальным API.
- Draft/applied-логика Configurator работает с `/api/generate`; provider выбирает
  только серверная переменная `mock/live`, а клиент не получает `FAL_KEY`.

**Slice 2 — довести оплату:** выбрать и подключить настоящего провайдера
(когда Артур решит), добавить проверку подписи вебхука, интерфейс покупки пакетов
на фронте.

**Slice 3 — каталог дисков и админка:** локальная функциональность готова. До
результатов реального bake-off её не расширять. Источники изображений — только
с подтверждёнными правами; **OEM-скрейпинг запрещён**.

**Slice 4 — полировка:** базовые аналитика и шаринг уже есть локально. i18n,
легальные страницы, модерация и новые продуктовые функции заморожены до результата
ступени 1 реального bake-off.

До результата ступени 1 разрешены только подготовка безопасного bake-off,
исправления, тесты, документация и описание будущего перехода на PostgreSQL.

### Категорически НЕ строить (явно отложено/вне проекта):
3D-сканирование машины (это ОТДЕЛЬНЫЙ будущий проект, не часть этого — Артур
специально это подчеркнул), мобильные приложения/Telegram-бот, настоящий
fitment-движок (bolt pattern и т.д.), marketplace/покупка дисков/affiliate,
подписки, автоматический AI-судья качества, любые геометрические модификации
(обвесы/спойлеры/оптика/занижение), собственная AI-модель, Python/CV-сервис.

---

## РАЗДЕЛ 5 — СЛЕДУЮЩИЕ ШАГИ

1. Не добавлять новые продуктовые функции до результата ступени 1 bake-off.
2. Первый живой запуск из приложения выполнен: потрачено $0.08. Пожизненный лимит
   Project Drive остаётся $5, дневной — $2; режим в `.env` по умолчанию `mock`.
3. Gemini API сверен 21.09.2026, адаптер переведён на текущий Interactions API.
4. Папка `bakeoff/new-photos/` и команда `photos:check` готовы. Сейчас есть 4 из
   15 фото; для ступени 1 уже хватает двух, полный набор требует ещё 11.
5. Текущий следующий шаг — принять или отклонить результат фазы A глазами Артура,
   затем выполнить фазу B строго в пределах бюджета из плана 23.09. Google-ключи
   и оба Google-аккаунта по-прежнему не использовать.

Удачи. Артур въедливый, дотошный к деталям и переживает за каждый доллар —
это хорошо, работай с ним в том же темпе, не гони вперёд его комфорта.
